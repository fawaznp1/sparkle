// Template for updating components - add this import and useTheme hook to each component:
// import { useTheme } from './ThemeContext';
// const { isDark } = useTheme();

// Replace these patterns:
// bg-gradient-to-br from-slate-900 to-slate-900 → ${isDark ? 'bg-gradient-to-br from-slate-900 to-slate-900' : 'bg-gradient-to-br from-gray-50 to-white'}
// text-white → ${isDark ? 'text-white' : 'text-gray-900'}
// text-white/90 → ${isDark ? 'text-white/90' : 'text-gray-700'}
// text-white/80 → ${isDark ? 'text-white/80' : 'text-gray-600'}
// bg-slate-800/40 → ${isDark ? 'bg-slate-800/40' : 'bg-white'}
// border-white/20 → ${isDark ? 'border-white/20' : 'border-gray-200'}
// bg-white/5 → ${isDark ? 'bg-white/5' : 'bg-gray-100'}

export default null;
